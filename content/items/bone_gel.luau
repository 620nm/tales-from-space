-- Sets a bone back together (tg stacks/medical.dm:753). A single tube
-- here rather than tg's stack -- the count is not what B2 needs.
return {
    id = "bone_gel", name = "bone gel", sprite = "bone_gel",
    tool = "bonegel", tool_speed = 1, surgical = true,
}
