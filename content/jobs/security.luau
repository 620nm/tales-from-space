-- Robust. The stun baton arrives with the combat gate; until then, a
-- security officer is a toolbox with a warrant.
return {
    id = "security",
    name = "Security Officer",
    slots = 1,
    uniform = "uniform_sec",
    id_card = "id_security",
    kit = { "toolbox" },
}
