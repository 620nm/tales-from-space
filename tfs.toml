# Tales from Space — mod manifest, read before the round's script host
# evaluates content (docs/SCRIPTING.md §5.1 identity, §7.2 loading).
# The id is
# vendor/mod — the vendor is the authoring group, which shares its name
# with the engine; the word "lunatic" otherwise names the engine only.

[mod]
id = "lunatic/tfs"
name = "Tales from Space"
version = "0.1.0"
api_version = 1
requires = []
optional = []
native_edges = [
    "movement.bump",
    "interaction.use",
    "attack.swing",
    "entity.pull",
    "throw.land",
    "matter.rupture",
    "body.life_state",
    "body.threshold",
    "part.seat",
    "part.unseat",
    "procedure.start",
    "procedure.operate",
    "lifecycle.mob",
]

[paths]
content = "content"
maps = "maps"
assets = "assets"
tests = "tests"

[permissions]
replace_handlers = true
prototype_handlers = true
