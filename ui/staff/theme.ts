// The staff package keeps its manifest-local style entry beside the guest
// entry. The actual rules live with the pack theme modules so the palette and
// component kit remain one source of truth.
export { default } from "../theme/staff";
